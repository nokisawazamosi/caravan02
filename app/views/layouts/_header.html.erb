
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="/">
      <p class="font-italic">CARAVAN</p>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to blogs_path, class:'nav-link' do %>
              <p class="font-italic">Home</p>
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to user_path(current_user.id), class:'nav-link' do %>
              <p class="font-italic">User</p>
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to new_blog_path, class:'nav-link' do %>
              <p class="font-italic">New Blog</p>
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to destroy_user_session_path, method: :delete, class:'nav-link' do %>
              <p class="font-italic">Log Out</p>
            <% end %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to new_user_registration_path, class:'nav-link' do %>
              <p class="font-italic">Sign Up</p>
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to new_user_session_path, class:'nav-link' do %>
              <p class="font-italic">Log In</p>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>







